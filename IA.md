# CPP 랜딩 페이지 정보구조 (IA)

한 화면(100dvh), 스크롤 없음, 배경 영상 풀블리드. 페이지의 단 하나의 목표는 **1차 CTA(사전 등록) 클릭**이다.
모든 보조 정보는 페이지 이탈 없이 오버레이(다이얼로그)로 연다.

---

## 0. 설계 원칙

| 원칙 | 적용 |
|---|---|
| 스크롤 없음 | `html, body { height:100% }` + `overflow:hidden`. 콘텐츠는 3행 그리드(상단 바 / 중앙 / 하단 바)에 고정 |
| 영상이 주인공 | UI는 영상을 가리지 않도록 가장자리에 배치. 중앙은 텍스트만 |
| 우선순위 | 헤드라인 → 1차 CTA → 브랜드 → 보조 정보 순으로 시각 위계 |
| 이탈 없는 보조 정보 | 소개·문의·사전 등록은 모두 `<dialog>`로 화면 위에 띄움 |
| 실패해도 무너지지 않음 | 영상 로드 실패/느린 네트워크 → 캔버스 그래디언트 대체 배경 |

---

## 1. 레이어 구조 (z-index 순)

```
L2.5 엔딩         영상 종료 시 페이드 인: "Stand with me" + CTA
L3-  재생 중 표시    하단 3px 반투명 진행 바 + 우하단 텍스트형 Skip (재생 중에만, 끝나면 페이드 아웃)
L3  오버레이       다이얼로그(사전 등록 / 소개 / 문의), 시네마 모드 닫기 버튼
L2  콘텐츠 그리드   상단 바 / 중앙 히어로 / 하단 바
L1  스크림         가독성용 그래디언트 (상·하단 + 좌측 어둡게)
L1.5 인트로        접속 직후 한 번: 블랙 배경 + 중앙 문구 → 블랙이 걷히며 영상 시작
L0  배경           <video autoplay muted playsinline> 1회 재생 후 마지막 프레임 정지 + 캔버스 대체 배경
```

---

## 2. 콘텐츠 그리드

### 2-1. 상단 바 (Header) — Figma 브랜드 아이덴티티 `Frame 14` (node 2248:57633) 그대로
| 위치 | 요소 | 규격 (1920 기준) |
|---|---|---|
| 바 | `nav.land-nav` | 1920×72, 배경 없음 (영상 위) |
| 좌 | Ludyte 로고 `assets/logo.svg` | x 80 / y 26, 90.6×20, 흰색 |
| 우 | `Explore` | 높이 40, 패딩 9/12, 라운드 8, 배경 **다크 와인 `#5A1A1F`**, 호버 시 **브랜드 레드 `#D0000E`**(Figma 원본 `#111927`은 흰 버튼 대비 작아 보여 교체. 대기 상태에 브랜드 레드를 쓰지 않는 이유: 1차 CTA인 흰 버튼보다 튀어 위계가 뒤집힘), 글자 `#f7f7f8`. 폭은 라벨 길이대로, 높이 40 고정 → https://ludyte.com/ 으로 이동 |
| 우 | `Join the expedition` (Figma 원본 라벨 `Sign up - It’s FREE`) | 높이 40, Explore와 같은 폭, 배경 `#fff`, 글자 `#171719`. 버튼 간격 8, 우측 여백 20 → **join.html로 이동** |
| 타이포 | body-base/semibold | DM Sans SemiBold 16px / 22px / 자간 -0.32px, opsz 14 |

Figma는 데스크톱만 정의되어 있어 태블릿·모바일은 좌측 여백만 20px로 줄이고(라이브 사이트와 동일) 나머지는 유지한다.
로고 SVG는 Figma 내보내기 결과에서 상위 프레임 배경 사각형 2개만 제거했다. 색 토큰은 `--nav-*`.

### 2-2. 중앙 히어로 (Hero) — 좌측 정렬, 최대 폭 760px
| 순서 | 요소 | 내용 규칙 |
|---|---|---|
| 1 | 아이브로우 | 라이브 점 + 상태 문구 (예: `CPP 2026 · 사전 등록 접수 중`). 모노 소문자, 대문자 변환 |
| 2 | 헤드라인 | 영문 디스플레이 1줄(세리프 이탤릭, 강조색) + 한글 1~2줄(고딕 볼드). 총 3줄 이내 |
| 3 | 서브카피 | 1~2문장, 34자 폭 이내. 무엇인지 + 왜 지금인지 |
| 4 | CTA 그룹 | **1차** `사전 등록` (채움 버튼, 다이얼로그) / **2차** `영상 전체 보기` (고스트 버튼, 시네마 모드) |

### 2-3. 하단 바 (Footer)
| 위치 | 요소 | 역할 |
|---|---|---|
| 좌 | 핵심 정보 3개 (`일정` / `장소` / `대상`) | 한 줄 사실. 라벨은 모노 대문자, 값은 고딕 |
| 우 | SNS 링크 2개 + 저작권 | 새 탭 링크 |

재생·일시정지 버튼, 타임코드, 사운드 토글은 없다. 배경 영상은 음소거 자동재생으로 **한 번만** 재생되고 끝나면 마지막 프레임에서 멈춘다(루프 없음).
재생 중에는 화면 맨 아래 3px 반투명 진행 바(매 프레임 갱신, 끊김 없음)와 우하단의 작은 텍스트형 `Skip ››` 버튼만 보인다. Skip을 누르면 영상을 끝으로 보내고 바로 엔딩(5절)을 띄운다. 영상이 끝나면 둘 다 0.6초 페이드 아웃. 소리는 시네마 모드에서만 켜지며, 시네마 모드 진입 시 처음부터 다시 재생한다.

---

## 3. 오버레이 (다이얼로그)

| ID | 트리거 | 구성 |
|---|---|---|
| `dlg-register` | (bare 모드에서 숨긴 히어로 CTA `사전 등록`) | 제목 / 설명 1줄 / 폼(이름·이메일·소속) / 제출 → 같은 자리에서 완료 상태로 전환 |
| `dlg-about` | (bare 모드에서 숨긴 히어로 `소개`; 상단 Explore는 ludyte.com 링크) | 제목 / 문단 2개 / 핵심 정보 재노출 |
| `dlg-contact` | 상단 `문의` | 이메일 주소(선택 가능한 텍스트) + `복사` 버튼. `mailto:`는 보조 |

공통: 닫기 버튼, 배경 클릭 닫기, `Esc` 닫기, 포커스 트랩은 `<dialog>` 기본 동작 사용.

---

## 4. 인트로 시퀀스 (접속 직후)

블랙 배경 위에 문구만 중앙에 놓이고, 끝나면 블랙이 걷히면서 영상이 이어진다. 영상은 이 시퀀스가 끝나는 순간부터 재생을 시작한다(그 전에는 0초에서 대기).

| 단계 | 시간 | 내용 |
|---|---|---|
| 문구 인 | 1.2초 | `They backed the vision. / They opened the way.` 엔딩 타이틀과 같은 서체·크기·페이드(투명 + 10px 상승) |
| 유지 | 1.0초 | |
| 문구 아웃 | 0.8초 | 문구만 페이드 아웃 |
| 블랙 아웃 | 1.2초 | 블랙 레이어가 걷히며 영상 재생 시작 (크로스페이드) |

- 총 약 3초. 상단 네비는 인트로 중에도 보이고 눌린다. 진행 바·Skip은 인트로가 끝나야 나타난다.
- 시간값은 스크립트의 `INTRO` 객체 한 곳에서 조정.
- 모바일은 문구 크기를 28~36px로 줄여 두 줄 문구가 과하게 꺾이지 않게 한다.

---

## 5. 엔딩 상태 (영상 종료 시)

영상이 끝나 마지막 프레임에서 멈추면 문구와 CTA가 **한 덩어리**로 페이드 인 된다. 덩어리(문구 + CTA)를 한 단위로 보고 가로·세로 모두 화면 정중앙에 놓는다.

| 요소 | 스타일 | 타이밍 |
|---|---|---|
| 문구 `Stand with me.` | Mona Sans SemiCondensed Bold(700), 46~91px (4.95vw), `#f7f7f8`. SemiBold(600)도 `assets/fonts`에 선언돼 있어 `font-weight`만 바꾸면 교체 | 1.2초 페이드 + 10px 상승 |
| CTA `Join the expedition →` | 문구 아래 28px. 상단 네비 Explore와 동일 규격(높이 40, 패딩 9/12, 라운드 8, DM Sans 16px/600), 흰색 배경. 호버 시 화살표 슬라이드 인(7절) | 문구보다 0.5초 늦게 1초 페이드 → **join.html로 이동** |

- 시네마 모드로 처음부터 다시 보면 엔딩이 사라졌다가, 끝나면 시네마 모드를 자동 종료하고 다시 나타난다.
- 대체 배경(캔버스)도 15초 뒤 같은 엔딩을 띄운다.
- `prefers-reduced-motion`이면 영상이 정지 상태이므로 엔딩 콘텐츠를 처음부터 보여준다.
- 노출 전에는 `inert`로 포커스·클릭이 막힌다.
- **하단 연무(WebGL)**: 엔딩이 뜨면 0.6초 뒤 2초에 걸쳐 화면 하단 42% 영역에 연무가 페이드 인. 바닥 전체에 고르게 깔리도록 균일한 바닥 층(최대의 60%) 위에 FBM 노이즈(4옥타브 + 워프) 결을 얹어 아주 느리게 위로 흐른다. 색은 푸른 회색에 브랜드 레드(#D0000E)의 탁한 톤을 은은하게 섞은 온도 그라디언트: 바닥 가장자리가 가장 따뜻하고(레드 약 17%) 위로 갈수록 원래 회색으로 돌아온다. 강도 16%, 밴딩 방지 디더. 라이브러리·에셋 없이 프래그먼트 셰이더로 그리고 텍스트·CTA 뒤 레이어. 절반 해상도, 모바일 40% 해상도·30fps 제한, 탭 숨김·시네마 모드·엔딩 해제 시 정지, WebGL 미지원이면 생략, reduce-motion이면 정지 프레임. 조정값은 `.stage`의 `--smoke-strength` / `--smoke-speed` / `--smoke-height` / `--smoke-warmth`(레드 가미, 0~1).
- 나중에 인물이 남는 컷으로 영상을 바꾸면 덩어리가 얼굴을 가릴 수 있으므로 `.ending-group`의 `top`만 내리면 된다.

---

## 6. 신청 페이지 `join.html` (CTA 이동처)

Figma "Ludyte CPP — Strategy / Landing Explorations" › `01 · The Invitation`(node 59:370)을 랜딩 톤앤매너로 워싱한 별도 페이지. 상단 `Join the expedition`과 엔딩 `Become a First Ally`가 모두 여기로 이동한다.

| 영역 | 구성 | 워싱 내용 |
|---|---|---|
| 네비 | 로고(랜딩으로 복귀) / 중앙 `EXPEDITION / FOUNDING EXPLORERS` / 우측 `Explore` 버튼(ludyte.com) | 랜딩과 같은 72px, 좌 80 / 우 20, **sticky**(스크롤해도 상단 고정, 배경 불투명). 시안의 `APPLICATION 01 / 05` 텍스트는 제거하고 랜딩 네비와 같은 Explore 버튼(40px, 라운드 8, 다크 와인, 호버 브랜드 레드)으로 대체 |
| 좌: 스토리 | 아이브로우 `A NEW CHAPTER STARTS HERE` / 타이틀 `Stand with me.` / 리드 / 본문 | 라임 아이브로우 → 무채색. 타이틀은 Mona Sans SemiCondensed Bold 52~82px, 본문 DM Sans |
| 우: 신청 카드 | `Join the expedition` / 필드 5개(이름·이메일·채널 URL·오디언스 규모·다음 챕터) / 동의 체크 / 제출 / 안내문 | 카드 `#f6f4ee` 라운드 24 유지. 필드 라운드 9 → 8. 라임 56px 필 버튼 → **브랜드 레드 `#D0000E`**(호버 `#B5000C`, 흰 글자, 대비 5.7:1) 48px 라운드 8. 라벨 DM Sans 700 12px 대문자 |
| 제출 | 필수: 이름·이메일·URL·동의 | 페이지 안에서 완료 상태(`Application received.`)로 전환, 랜딩 복귀 버튼. 전송처 미연결 |
| 반응형 | ≥1024 2단(우측 최대 638px) / 1023 이하 1단 / ≤599 네비 중앙 라벨 숨김 | 이 페이지는 세로 스크롤 허용 |

---

## 7. 버튼 마이크로 인터랙션 (slide-arrow)

상단 `Join the expedition`과 엔딩 `Join the expedition`에 적용(`.btn-slide`). Explore는 배경색 호버만 있고 이 인터랙션은 넣지 않는다.

| 상태 | 동작 |
|---|---|
| 대기 | 텍스트만. 화살표 영역 폭 0 |
| 호버 | 우측에서 화살표가 10px 슬라이드 인 + 폭 0→22px, 버튼 scale 1.02. Framer Motion spring(stiffness 600, damping 25)을 CSS `linear()` 곡선으로 재현(0.5초, 약간의 오버슈트) |
| 클릭 | scale 0.96 |
| 터치 기기 | 호버가 없으므로 화살표 상시 노출, 스케일 없음 |
| reduce-motion | 전환 없음 |

---

## 8. 시네마 모드 (2차 CTA)

- `영상 전체 보기` 클릭 → 콘텐츠 그리드와 스크림이 페이드 아웃, 영상이 처음부터 소리 켜져 재생 (끝나면 역시 마지막 프레임 정지)
- 우상단 `닫기 ESC` 버튼과 화면 하단 2px 진행선만 남김
- `Esc` 또는 닫기 → 원래 화면 복귀, 다시 음소거

---

## 9. 상태 & 예외

| 상황 | 동작 |
|---|---|
| 영상 로드 실패 (404 등) | `<video>` 숨기고 캔버스 그래디언트 15초 재생 후 정지 |
| 2.5초 내 첫 프레임 없음 | 우선 대체 배경 표시, 영상 도착 시 자동 교체 |
| `prefers-reduced-motion` | 영상 자동재생 중지(첫 프레임 정지), 캔버스는 정지 프레임, 펄스·트랜지션 제거 |
| 세로 화면 (모바일) | `data-src-portrait`(9:16) 소스로 전환. 가로↔세로 회전 시 재선택 |
| 낮은 높이 (가로 모드 폰, ≤560px) | 서브카피·핵심 정보 숨김, 헤드라인 축소 |
| 좁은 폭 (≤720px) | 상단 텍스트 링크 숨김(아이콘 버튼만), 하단 바 세로 스택 |

---

## 10. 브레이크포인트

| 구간 | 폭 | 거터 | 상단 바 | 히어로 | 하단 바 |
|---|---|---|---|---|---|
| 모바일 | ≤ 599px | 18px | 좌 16 / 우 12, 버튼 글자 14px·패딩 10px(높이 40 유지), 네비 화살표 생략. 360px에서도 한 줄에 들어감 | 헤드라인 28~40px, CTA 세로 풀폭 | 세로 스택. 핵심 정보 3열 그리드 |
| 태블릿 | 600 – 1023px | 28px | 동일, 좌 20 | 최대 폭 640px, 헤드라인 34~56px | 2행: 핵심 정보 / SNS |
| 데스크톱 | ≥ 1024px | 16~44px (3.5vw) | Figma 원본 (좌 80 / 우 20) | 최대 폭 760px, 헤드라인 30~68px | 1행 2열 |
| 낮은 높이 | 높이 ≤ 560px | — | — | 서브카피 숨김, 헤드라인 축소, CTA 가로 | 핵심 정보 숨김 |
| 터치 기기 | `hover: none` | — | hover 상태 제거 | — | — |

```
데스크톱 (≥ 1024)                 태블릿 (600–1023)             모바일 (≤ 599)
┌────────────────────────┐        ┌──────────────────┐          ┌────────────┐
│ CPP      소개 문의 [🔊] │        │ CPP  소개 문의 [🔊]│          │ CPP   [🔊] │
│                        │        │                  │          │            │
│ ● 접수 중               │        │ ● 접수 중         │          │ ● 접수 중   │
│ One screen.            │        │ One screen.      │          │ One screen.│
│ 스크롤 없이, 한 편의 영상으로.│    │ 스크롤 없이,      │          │ 스크롤 없이, │
│ 서브카피                │        │ 한 편의 영상으로.  │          │ 한 편의     │
│ [사전 등록] [영상 보기]   │        │ 서브카피          │          │ 영상으로.   │
│                        │        │ [사전 등록][영상 보기]│        │ [사전 등록] │
│ 일정 장소 대상 ▶── 00:07 SNS│     │ 일정  장소  대상   │          │ [영상 보기] │
└────────────────────────┘        │ ▶──── 00:07   SNS │          │ 일정 장소 대상│
                                  └──────────────────┘          │ ▶──── 00:07│
                                                                │ SNS     ©  │
                                                                └────────────┘
```

### bare 모드 (현재 상태)
`<div class="stage bare">`의 `bare` 클래스가 히어로 텍스트·CTA·하단 정보를 숨긴다. 남는 것: 상단 네비(브랜드 + Explore + Sign up)뿐.
클래스를 지우면 위 구조가 그대로 복구된다.

---

## 11. 에셋 체크리스트

| 파일 | 규격 | 비고 |
|---|---|---|
| `assets/hero-16x9.mp4` | 1920×1080 이상, H.264, 10~30초 | **연결됨** — `rotation-ramp-v16-lossless.mov`(qtrle RGB 무손실, 4.46GB, 브라우저 재생 불가)에서 직접 인코딩. 2560×1440, 60fps, 9.95초, 무음. 로컬: CRF 16(16.5Mbps, 20.5MB). 아티팩트: `hero-16x9-crf19.mp4`(11.3Mbps, 14.0MB, 15MB 한도). 이전 AME 출력본은 `hero-v16-ame.mp4` 등으로 보관 |
| `assets/hero-9x16.mp4` | 1080×1920, 동일 조건 | 미연결 — 세로 화면은 현재 16:9 영상을 중앙 크롭 |
| `assets/poster.jpg` | 1920×1080, 200KB 이하 | **연결됨** — 0.5초 지점 프레임 |
| `assets/logo.svg` | 91×20 | **연결됨** — Figma node 2248:57634 내보내기 |
| `assets/fonts/MonaSans_SemiCondensed-{SemiBold,Bold}.ttf` | TTF | **연결됨** — 엔딩 타이틀 서체 (self-host) |
| 로고 SVG | 단색 | 현재는 원형 마크 + 워드마크로 대체 |

`index.html`의 `<video data-src-landscape data-src-portrait poster>` 경로만 바꾸면 연결된다.

---

## 12. 교체해야 할 카피 슬롯

표기 규칙: 버튼·문장은 문장식(Join the expedition, Send my application). "Founding Explorer"는 참가자 호칭이라 대문자 유지. 대문자 라벨(A NEW CHAPTER STARTS HERE 등)은 CSS 변환.

| 슬롯 | 현재 임시 문구 |
|---|---|
| 아이브로우 | CPP 2026 · 사전 등록 접수 중 |
| 헤드라인 (영문) | One screen. |
| 헤드라인 (한글) | 스크롤 없이, 한 편의 영상으로. |
| 서브카피 | CPP의 첫 시즌을 영상 한 편으로 소개합니다. 지금 사전 등록하고 가장 먼저 소식을 받아보세요. |
| 핵심 정보 | 일정 2026.10.15 – 11.30 / 장소 서울 · 온라인 동시 / 대상 크리에이터 · 스튜디오 |
| 인트로 문구 | They backed the vision. / They opened the way. |
| 엔딩 문구 | Stand with me. |
| 엔딩 CTA | Join the expedition |
| 상단 CTA | Join the expedition |
| 문의 이메일 | hello@cpp.example |
